import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'flex-start',
        paddingTop: 150,
    },
    text: {
        fontWeight: 'bold',
        fontSize: 30,
        color: 'black',
        marginBottom: 40,
    },
    inputContainer: {
        marginBottom: 30
    }
});